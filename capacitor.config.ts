import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'net.michaelcalvinwood.ionicrelaxationtimer',
  appName: 'ionic-relaxation-timer',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
