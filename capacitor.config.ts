import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'net.michaelcalvinwood.ionicrelaxationtimer',
  appName: 'Rapid Relaxation Timer',
  webDir: 'build',
  bundledWebRuntime: false,
};

export default config;
